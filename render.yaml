services:
  - type: web
    name: real-school-bot
    env: python
    plan: free
    region: oregon
    buildCommand: pip install -U pip && pip install -r requirements.txt
    startCommand: python bot.py
